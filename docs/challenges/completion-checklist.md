# Completion Checklist

Mark each item when complete.

- [ ] CLI help runs.
- [ ] Sample finance dataset validates.
- [ ] Alpaca sample converts to normalized JSONL.
- [ ] Training dry run writes `run.json`.
- [ ] Evaluation writes JSON and HTML reports.
- [ ] GGUF export dry run writes an export manifest.
- [ ] Ollama dry run writes a Modelfile.
- [ ] Dashboard writes `index.html`.
- [ ] Hyperparameter sweep plan creates eight runs.
- [ ] Model comparison includes Qwen, Llama, Gemma, and DeepSeek.
- [ ] `python -m pytest` passes.
- [ ] `python -m ruff check .` passes.

Generated files belong under `experiments\` or `exports\`.
